# docs is a standalone package (not a real workspace). This file exists only to allow the
# native build scripts that Starlight needs; pnpm v11 blocks them by default.
allowBuilds:
  esbuild: true
  sharp: true
