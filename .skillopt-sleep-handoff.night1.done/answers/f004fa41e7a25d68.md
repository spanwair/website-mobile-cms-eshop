[{"op": "add", "content": "When the user points to a local folder for a package instead of requesting a package manager install, use the appropriate environment variable (e.g. SKILLOPT_SLEEP_REPO) to reference that path and proceed without pip/uv.", "rationale": "Ensures local paths are consumed via env vars rather than triggering an install."}]
