{
  "cli": { "version": ">= 16.0.0" },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "android": { "buildType": "apk", "env": { "APP_ENV": "development" } }
    },
    "production": {
      "distribution": "store",
      "android": { "buildType": "apk", "env": { "APP_ENV": "production" } }
    }
  }
}
